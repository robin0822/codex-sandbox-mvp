--- a/ext/skills/src/extension.rs
+++ b/ext/skills/src/extension.rs
@@ -486,6 +486,7 @@
                         };
                         fragments.push(Box::new(fragment));
+                        eprintln!("CODEX_MVP_SKILL_LOADED\t{}", entry.name);
                         main_prompts_injected = true;
                         if entry.authority.kind == SkillSourceKind::Host {
